# DEV History — Phantombite AdminProjektor

## 2026-09-19 — Bereinigung
- Compile-Fehler behoben: fehlendes `using VRage.Game;` (`MyOwnershipShareModeEnum`)
- Log-Meldungen der Block-Logik werden nach dem vom Core gesetzten Log-Level gefiltert (vorher jede Meldung an den Core)
- Doku angelegt (`DEV_Funktion.md`), `.gitignore` ergänzt
